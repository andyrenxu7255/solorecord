# SoloRecord

## 中文

> 公司内部会议记录系统：Android APK + Windows/macOS/iOS/HarmonyOS 终端外壳 + FastAPI 服务端 + Web 管理端 + 本地或远程 STT/ASR + LLM 流水线 + 群晖 LDAP/SSO + 服务端转写/纪要存储 + 可选 ES/OpenSearch。

### 先读这里

- [智能体指南](AGENTS.md)：关键规则、构建/测试命令、架构地图。
- [README](README.md)：项目概览和快速开始。

### 人类运维

- [运维手册](docs/human-ops/README.md)：部署、环境变量、LDAP/SSO、ASR/LLM、ES、备份、APK 发布、排障。
- [部署参考](docs/deployment.md)：快速启动和 Docker 说明。
- [安全审计](docs/security-audit.md)：安全控制、原型风险、验证结果。
- [多终端客户端](docs/multi-platform-clients.md)：Android、Windows、macOS、iOS、HarmonyOS 构建与发布。
- [数据存储与 ES](docs/data-storage-and-es.md)：服务端权威存储、移动端重装同步、外部 API、ES/OpenSearch 文档结构。

### 人类开发

- [开发者手册](docs/human-dev/README.md)：仓库结构、模块、API、数据模型、测试、Android/Web/Server 开发。
- [集成契约](docs/integration-contract.md)：ASR/LLM/Webhook 接口参考。
- [本地 ASR 流水线](docs/local-asr-pipeline.md)：音频处理、VAD、说话人分离、ASR 流水线决策。

### 普通使用者

- [使用手册](docs/user/README.md)：登录、录音、同步、记录、说话人改名、Web 使用、导出、FAQ。

### 智能体维护

- [智能体维护手册](docs/agents/README.md)：上下文摘要、安全任务模式、已知风险、文档维护规则。
- 服务器运维 Agent 自动部署：见 [docs/agents/README.md](docs/agents/README.md) 的“服务器运维 Agent 自动部署 Runbook”，会向操作者收集 URL/key，写入服务器 `.env`，自动部署、发布终端应用并联调。

### 产品与架构参考

- [PRD](docs/meeting-app-prd-v2.md)：产品范围、UX、架构、数据模型、验收标准。
- [群晖 SSO + ASR 架构](docs/synology-bailian-architecture.md)：安全决策、群晖 SSO、百炼/Qwen ASR 分析。
- [开源调研](docs/open-source-research.md)：sherpa-onnx、whisper.cpp、Vosk、Whishper、Meetily、许可证注意事项。
- [分发模式](docs/distribution-mode.md)：内部 APK 分发参考。
- [多终端客户端](docs/multi-platform-clients.md)：多平台终端工程、构建、签名与服务端发布。
- [Roadmap](docs/roadmap.md)：后续增强建议。
- [V0.7 发布说明](docs/release-v0.7.md)：当前发布能力、验证状态和部署提醒。

### 代码地图

- `server/solorecord_server/main.py`：FastAPI 路由。
- `server/solorecord_server/db.py`：SQLite schema 和迁移。
- `server/solorecord_server/auth.py`：会话、LDAP、SSO、外部 token 鉴权。
- `server/solorecord_server/processing.py`：ASR、纪要、转发、索引流程。
- `processing.py` 在 ASR 后执行语义分段：优先使用 ASR 原生 speaker 字段；若只有单一发言人、长段、多人标记，或虽有多个原生 speaker 但出现“某某你先说/我这边负责/某某负责/某某确认/某某后面看”这类上下文归属线索，会调用 LLM 重分段；LLM 输入和输出都应保留 `source_index`/`source_id`/`source_segment_no`，保证最终段落可追溯到原始录音源和音频分段；多源同录先按 `(source_id, source_segment_no)` 保留证据，最终处理会合并重复且关键事实一致的多源片段并标记 `multi_source_merged`，设备错峰起录但来源分段相邻、时间差和文本相似度满足保守阈值时额外标记 `multi_source_time_aligned`，多个来源各自漏词但无关键事实冲突时可复制原始转写短语并标记 `multi_source_complemented`，日期、数量或负责人冲突的片段标记 `multi_source_conflict`；LLM 返回后仍有明显多人点名长段时，`_rule_refine_residual_mixed_segments()` 会二次规则细拆并保留 `llm_residual_rule_refined`、`speaker_review`；LLM 不可用时用规则拆“某某说/某某：”，并保守处理被点名后的回应归属，包括 ASR 无标点连接的“某某你先说...我这边...”和没有“我”字但继续同一议题、交付物或时间节点的段落。
- `server/solorecord_server/repository.py`：完整会议文档聚合。
- `repository.py` 的 `qualityReport` 是转写/纪要质量兜底层，包含发言人校对、`speakerEvidence` 发言人推断依据、发言人证据弱提示、同名多标签 `speakerAliasConflicts`、按 `(source_id, source_segment_no)` 计算的 `sourceCoverage` 原始音频分段覆盖检查、多源合并/冲突指标、`summaryEvidence.supportedClaims/unsupportedClaims` 纪要证据引用、`actionEvidence` 每条待办证据状态和可选 `suggested_owner`、候选人名、待办负责人集中度、待办证据覆盖率和待办负责人证据弱提示；证据引用应带 `segment_id`、`source_id` 和 `source_segment_no`，Web 用“定位转写”跳回原文，外部 Agent 用它做证据追溯。`knowledgeReadiness` 是外部 Agent 入库门禁摘要。`processing._normalize_action_owners()` 会把“我/我们/他/这边/大家”等代词 owner 尽量换成有第一人称转写证据的具体发言人，否则降级为 `待确认`；如果主责明确但转写出现“配合/协同/补充”等协作关系，会在 task 末尾追加 `协同：姓名`。分段阶段转写带 `semantic_partial`；结束会议后的整场上下文复核带 `semantic_final`。`suggested_owner` 只能作为人工复核建议，不能自动覆盖用户待办。`source_segment_coverage_weak` 是知识入库阻塞项；`multi_source_conflict_count`、`speaker_evidence_weak_count`、`speaker_alias_conflict_count`、`unsupported_action_count` 或 `weak_action_owner_count` 大于 0 时需要人工复核。
- `knowledgeGraph` 包含 meeting、speaker、topic、action、time 节点；topic 来自转写和待办的轻量抽取，用于连接“谁讨论了什么、产生了什么待办、何时截止”。topic 节点的 `evidence` 必须保留 `segment_id`、`source_id`、`source_segment_no`、`start_ms`、`end_ms` 和原文片段。图谱是查阅和外部知识整理辅助，不替代转写证据。日期/截止时间短语（如“今天下午”“周三前”“月底前”）只能作为 time/topic 事实，不能作为 speaker 或 owner；销售、法务、前端、测试等组织角色可以作为 action owner，但必须有同一短语窗口内的责任或动作证据，不能把“自动测试、测试覆盖、数据源”等任务词当成团队负责人。
- `server/solorecord_server/search_index.py`：ES/OpenSearch 索引。
- `server/static/index.html`：Web 外壳。
- `server/static/app.js`：Web 交互。
- `server/tests/test_api.py`：主 API 回归测试。
- `app/src/main/java/com/solorecord/MainActivity.java`：Android UI 和流程。
- `app/src/main/java/com/solorecord/net/RollingAudioRecorder.java`：Android 滚动录音。
- `app/src/main/java/com/solorecord/net/SoloServerClient.java`：Android 服务端 API、同步、服务器音频下载。
- `clients/desktop`：Windows/macOS Electron 客户端。
- `clients/ios`、`clients/macos`、`clients/harmony`：iOS、macOS、HarmonyOS WebView 外壳工程。

### 常用命令

```powershell
scripts\run-tests.ps1
scripts\run-server.ps1
& 'C:\Users\Andy\.gradle\wrapper\dists\gradle-8.7-bin\bhs2wmbdwecv87pi65oeuq5iu\gradle-8.7\bin\gradle.bat' assembleDebug
cd clients\desktop; npm install; npm run pack; npm run portable:win
```

### 公开仓库边界

源码和文档可以公开，但真实密钥、`server/.env`、运行数据、数据库、缓存、终端构建产物和客户会议音频不能提交。生产部署仍按公司内部系统保护。

## English

> Internal company meeting recorder: Android APK + Windows/macOS/iOS/HarmonyOS client shells + FastAPI server + Web admin + local or remote STT/ASR + LLM pipeline + Synology LDAP/SSO + server-side transcript/summary storage + optional ES/OpenSearch.

## Start Here

- [Agent Guide](AGENTS.md): Critical rules, build/test commands, architecture map.
- [README](README.md): Project overview and quick start.

## Human Operations

- [Operations Manual](docs/human-ops/README.md): Deployment, env vars, LDAP/SSO, ASR/LLM, ES, backups, APK publishing, troubleshooting.
- [Deployment Reference](docs/deployment.md): Quick start and Docker notes.
- [Security Audit](docs/security-audit.md): Security controls, prototype risks, verification results.
- [Multi-platform Clients](docs/multi-platform-clients.md): Android, Windows, macOS, iOS, and HarmonyOS builds and publishing.
- [Data Storage And ES](docs/data-storage-and-es.md): Server authoritative storage, mobile resync, external API, ES/OpenSearch document shape.

## Human Development

- [Developer Manual](docs/human-dev/README.md): Repository structure, modules, API, data model, tests, Android/Web/server development.
- [Integration Contract](docs/integration-contract.md): Legacy ASR/LLM/webhook contract notes.
- [Local ASR Pipeline](docs/local-asr-pipeline.md): Audio processing, VAD, diarization, ASR pipeline decisions.

## End Users

- [User Manual](docs/user/README.md): Login, recording, sync, records, speaker rename, Web use, export, FAQ.

## Agent Maintenance

- [Agent Maintenance Manual](docs/agents/README.md): Context summary, safe task patterns, known risks, documentation maintenance rules.
- Server operations agent auto-deployment: see the "Server Operations Agent Auto-Deployment Runbook" in [docs/agents/README.md](docs/agents/README.md). It collects URLs/keys from the operator, writes server-local `.env`, deploys, publishes client packages, and runs integration checks.

## Product And Architecture References

- [PRD](docs/meeting-app-prd-v2.md): Product scope, UX, architecture, data model, acceptance criteria.
- [Synology SSO + ASR Architecture](docs/synology-bailian-architecture.md): Security decision, Synology SSO, Bailian/Qwen ASR analysis.
- [Open Source Research](docs/open-source-research.md): sherpa-onnx, whisper.cpp, Vosk, Whishper, Meetily, licensing notes.
- [UX User Story Acceptance](docs/ux-user-story-acceptance.md): Mainstream meeting-recorder benchmark, end-to-end user stories, and UX acceptance checklist.
- [UI/UE Design System](docs/ui-ue-design-system.md): Office-role UI principles, Web/Android design rules, and visual acceptance criteria.
- [Distribution Mode](docs/distribution-mode.md): Original internal-distribution notes.
- [Multi-platform Clients](docs/multi-platform-clients.md): Client projects, builds, signing, and server-side publishing.
- [Roadmap](docs/roadmap.md): Follow-up enhancement ideas.

## Code Map

- `server/solorecord_server/main.py`: FastAPI routes.
- `server/solorecord_server/db.py`: SQLite schema and migrations.
- `server/solorecord_server/auth.py`: Sessions, LDAP, SSO, external token auth.
- `server/solorecord_server/processing.py`: ASR, summary, forwarding, indexing workflow.
- `processing.py` runs post-ASR semantic segmentation: prefer native ASR speaker fields, otherwise use LLM refinement, then a residual rule pass for clear mixed-person markers that the LLM left in place. LLM input/output must preserve `source_index`, `source_id`, and `source_segment_no`. Multi-source meetings keep evidence by `(source_id, source_segment_no)`; final processing merges duplicate rows with `multi_source_merged` only when key facts agree, can additionally mark `multi_source_time_aligned` when devices started at different times but source-local segment numbers, start delta, and text similarity match, can copy non-conflicting phrases from original transcript rows with `multi_source_complemented`, and flags divergent rows or date/amount/owner conflicts with `multi_source_conflict`. The residual pass writes `llm_residual_rule_refined` and `speaker_review`. Named call-outs followed by first-person or same-topic continuation replies can produce `contextual_speaker_inference`, including ASR text that lacks punctuation between the call-out and reply; keep `speaker_review` and `reason:*` because this is contextual evidence, not voiceprint confirmation.
- `server/solorecord_server/repository.py`: Full meeting document aggregation.
- `repository.py` builds the quality-report safety layer: speaker review, weak speaker evidence, same-name/multiple-label `speakerAliasConflicts`, candidate people, owner concentration, and action evidence coverage. `processing._normalize_action_owners()` converts pronoun owners such as “I/we/he/this side/everyone” to concrete speakers only when first-person transcript evidence supports it; otherwise it keeps `待确认`. Review meetings when `speaker_evidence_weak_count`, `speaker_alias_conflict_count`, or `unsupported_action_count` is above zero.
- `knowledgeGraph` contains meeting, speaker, topic, action, and time nodes. Topic nodes come from lightweight transcript/action extraction and connect who discussed what, which action was produced, and when it is due. Topic-node `evidence` must keep `segment_id`, `source_id`, `source_segment_no`, `start_ms`, `end_ms`, and the transcript snippet. The graph helps review and downstream knowledge organization; it does not replace transcript evidence. Date and deadline phrases are time/topic facts only, not speakers or owners. Organizational roles such as sales, legal, frontend, and QA may be action owners only when explicit assignment/action evidence appears in the same short phrase; task nouns such as automated testing, test coverage, or data source are not enough by themselves.
- `server/solorecord_server/quality_probe.py`: Read-only real-meeting LLM quality probe. Use `python -m solorecord_server.quality_probe --meeting-id <id>` and optionally `--run-llm`; it must not rewrite transcripts or action items.
- `server/solorecord_server/search_index.py`: ES/OpenSearch indexing.
- `server/static/index.html`: Web shell.
- `server/static/app.js`: Web behavior.
- `server/tests/test_api.py`: Main API regression test.
- `app/src/main/java/com/solorecord/MainActivity.java`: Android UI and workflow.
- `app/src/main/java/com/solorecord/net/RollingAudioRecorder.java`: Android rolling recorder.
- `app/src/main/java/com/solorecord/net/SoloServerClient.java`: Android server API client, mobile sync, server audio download.
- `clients/desktop`: Windows/macOS Electron client.
- `clients/ios`, `clients/macos`, `clients/harmony`: iOS, macOS, and HarmonyOS WebView shells.

## Commands

```powershell
scripts\run-tests.ps1
scripts\run-server.ps1
& 'C:\Users\Andy\.gradle\wrapper\dists\gradle-8.7-bin\bhs2wmbdwecv87pi65oeuq5iu\gradle-8.7\bin\gradle.bat' assembleDebug
cd clients\desktop; npm install; npm run pack; npm run portable:win
```
